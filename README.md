# SSyslog
Syslog server that supports SSH/TLS

# How to use
  - Run UI application by `python src/advanced_syslog_server_UI.py`
  - To use security feature (TLS), need to generate certificate + key for server

# CI
  - In order to test Syslog server, possible to use a client at `tst/syslog_client.py`
